export const archiveRounds = [
  // Completed rounds are added here before the site is moved on to the next round.
  // Predictions themselves remain stored in Vercel Blob; this file only preserves
  // each completed round's fixture details and final scores so the archive can
  // rebuild the full match-by-match breakdown at any time.
];
